// Flutter imports:
import 'package:flutter/material.dart';

class RelatedWorksTab extends StatelessWidget {
  const RelatedWorksTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('関連作品'),
    );
  }
}
